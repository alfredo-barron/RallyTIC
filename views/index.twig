{% extends 'base.twig' %}

{% block content %}
  <div class="section no-pad-bot" id="index-banner">
    {% if user.rol == "O" %}
      <div id="tab1" class="col s12 m12">
        {% include 'tab1.twig' %}
      </div>
      <div id="tab2" class="col s12 m12">
        {% include 'tab2.twig' %}
      </div>
      <div id="tab3" class="col s12 m12">
        {% include 'tab3.twig' %}
      </div>
      <div id="tab4" class="col s12 m12">
        {% include 'tab4.twig' %}
      </div>
    {% elseif user.rol == "P" %}
      <div id="tab5" class="col s12 m12">
        {# {% include 'tab5.twig' %} #}
      </div>
      <div id="tab6" class="col s12 m12">
        {% include 'tab6.twig' %}
      </div>
      <div id="tab7" class="col s12 m12">
        {# {% include 'tab7.twig' %} #}
      </div>
    {% endif %}
    <br>
  </div>
{% endblock %}
{% block js %}
  <script type="text/javascript" src="//maps.google.com/maps/api/js?sensor=true"></script>
  <script src="{{ asset('gmaps.js') }}"></script>
  <script type='text/javascript'>
    $(document).ready(function() {
      var map;
      map = new GMaps({
        div: '#map',
        lat: 22.0030923,
        lng: -99.0203763,
        enableNewStyle: true,
        zoom: 15
      });

      GMaps.geolocate({
        success: function(position) {
          map.setCenter(position.coords.latitude, position.coords.longitude);
        }
      });
    });
  </script>
{% endblock %}
{% block css %}
    <style>
  #map{
    height: 500px;
  }
  </style>
{% endblock %}
